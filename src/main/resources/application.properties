spring.application.name=Sparta Logistics India
spring.security.user.name=admin
spring.security.user.password=changeMe
spring.security.user.roles=USER

# Default "from" address used in outgoing mail. If not set, will fall back to spring.mail.username or no-reply@sparta.com
spring.mail.from=ranjana.adhikari@spartalogisticsindia.in

# --------------------------------------------------------------------------------
# Recommended SMTP providers (choose one).
# --------------------------------------------------------------------------------

# SendGrid (recommended for straightforward transactional mail)
spring.mail.host=smtp.sendgrid.net
spring.mail.port=587
spring.mail.username=apikey
# Do NOT hardcode API keys. Provide at runtime via environment variable SENDGRID_API_KEY.
spring.mail.password=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
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.sendgrid.net
spring.mail.default-encoding=UTF-8
# Disable automatic test-connection at startup to avoid IllegalStateException if SMTP is temporarily unreachable
spring.mail.test-connection=false
spring.mail.properties.mail.debug=${MAIL_DEBUG:false}

# Recipient for enquiry notifications (controller reads enquiry.notification.recipient)
enquiry.notification.recipient=radhikari54@gmail.com

# Mail configuration fallback keys used by custom SmtpMailSender if needed
mail.username=${spring.mail.username:apikey}
mail.password=${spring.mail.password:}
mail.host=${spring.mail.host:smtp.sendgrid.net}
mail.port=${spring.mail.port:587}
mail.starttls=true
mail.auth=true
mail.ssl.trust=${spring.mail.properties.mail.smtp.ssl.trust:smtp.sendgrid.net}
mail.debug=${spring.mail.properties.mail.debug:false}
